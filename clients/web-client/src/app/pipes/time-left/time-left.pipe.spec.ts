import {TimeMinutesLeftPipe} from "./time-minutes-left.pipe";

describe("TimeMinutesLeftPipe", () => {
    it("create an instance", () => {
        const pipe = new TimeMinutesLeftPipe();
        expect(pipe).toBeTruthy();
    });
});
