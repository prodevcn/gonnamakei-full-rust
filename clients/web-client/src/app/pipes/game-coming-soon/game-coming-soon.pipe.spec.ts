import {GameComingSoonPipe} from "./game-coming-soon.pipe";

describe("GameComingSoonPipe", () => {
    it("create an instance", () => {
        const pipe = new GameComingSoonPipe();
        expect(pipe).toBeTruthy();
    });
});
