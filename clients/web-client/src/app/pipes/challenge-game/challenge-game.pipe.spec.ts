import {ChallengeGamePipe} from "./challenge-game.pipe";

describe("ChallengeGamePipe", () => {
    it("create an instance", () => {
        const pipe = new ChallengeGamePipe();
        expect(pipe).toBeTruthy();
    });
});
