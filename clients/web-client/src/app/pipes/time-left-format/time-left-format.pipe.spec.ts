import {TimeLeftFormatPipe} from "./time-left-format.pipe";

describe("TimeLeftFormatPipe", () => {
    it("create an instance", () => {
        const pipe = new TimeLeftFormatPipe();
        expect(pipe).toBeTruthy();
    });
});
